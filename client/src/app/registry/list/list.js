'use strict';

angular.module('registry.list', ['res.api', 'svc.notifications', 'svc.busy'])

.config(function ($routeProvider) {

})

.controller('RegListCtrl', function ($scope, $rootScope, api, $location, notifications, busy) {

});